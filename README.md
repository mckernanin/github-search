# Github Search

## Setup

Install packages with `yarn` in the root directory (not necessary to do this for each sub folder)

To run the backend:
`yarn backend start`

To run the frontend:
`yarn frontend start`
